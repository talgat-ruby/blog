// const chalk = require('chalk');
// const db = require('>/src/db/');

// let client;
// const testUser = {};

// beforeAll(async () => {
// 	try {
// 		client = await db.connect();
// 		client.query(`
// 			INSERT INTO
// 		`);
// 	} catch (e) {
// 		console.log(chalk.red(e));
// 	}
// });

// afterAll(() => client.release());

// test('Check the test user', async () => {});
